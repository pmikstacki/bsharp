---
import { Icon } from 'astro-icon/components';
---
<section id="features" class="section hue-amber">
  <div class="mx-auto w-full max-w-7xl grid grid-cols-1 gap-8 px-4 py-24 sm:grid-cols-2 lg:grid-cols-4 md:px-8">
  <a href="#parser" class="group glass enable-trig trig-fade-up p-6 md:p-7" data-section-color="emerald">
    <div class="flex items-center gap-3">
      <Icon name="lucide:code-2" class="h-5 w-5 section-icon" />
      <h3 class="font-semibold section-title">Parser</h3>
    </div>
    <p class="mt-2 text-sm section-description">Robust C# grammar with deterministic worldgen-friendly principles. Foundation for all tooling.</p>
  </a>
  <a href="#formatter" class="group glass enable-trig trig-fade-up p-6 md:p-7" data-section-color="indigo">
    <div class="flex items-center gap-3">
      <Icon name="lucide:square-pen" class="h-5 w-5 section-icon" />
      <h3 class="font-semibold section-title">Formatter</h3>
    </div>
    <p class="mt-2 text-sm section-description">Opinionated, stable formatting with predictable diffs and configurable profiles.</p>
  </a>
  <a href="#analysis" class="group glass enable-trig trig-fade-up p-6 md:p-7" data-section-color="orange">
    <div class="flex items-center gap-3">
      <Icon name="lucide:bar-chart-3" class="h-5 w-5 section-icon" />
      <h3 class="font-semibold section-title">Analysis</h3>
    </div>
    <p class="mt-2 text-sm section-description">Static analysis pipeline with precise control-flow, dependencies, and metrics.</p>
  </a>
  <a href="#compiler" class="group glass enable-trig trig-fade-up p-6 md:p-7" data-section-color="purple">
    <div class="flex items-center gap-3">
      <Icon name="lucide:cpu" class="h-5 w-5 section-icon" />
      <h3 class="font-semibold section-title">LLVM Compiler</h3>
    </div>
    <p class="mt-2 text-sm section-description">Planned LLVM-based backend for performance and portability.</p>
  </a>
  </div>
</section>

<style>
  /* Section-specific colors for feature tiles */
  [data-section-color="emerald"] .section-icon { color: #10B981; }
  [data-section-color="emerald"] .section-title { color: #4edea3; }
  [data-section-color="emerald"] .section-description { color: #c0c9c1; }
  
  [data-section-color="indigo"] .section-icon { color: #3949AB; }
  [data-section-color="indigo"] .section-title { color: #bbc3ff; }
  [data-section-color="indigo"] .section-description { color: #e4e1e6; }
  
  [data-section-color="orange"] .section-icon { color: #FF9800; }
  [data-section-color="orange"] .section-title { color: #ffb870; }
  [data-section-color="orange"] .section-description { color: #ebe0d9; }
  
  [data-section-color="purple"] .section-icon { color: #9C27B0; }
  [data-section-color="purple"] .section-title { color: #e9d5ff; }
  [data-section-color="purple"] .section-description { color: #f3e8ff; }
</style>
